<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:layout_width="120dp"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:layout_margin="10dp">

    <include android:id="@+id/person_image"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        layout="@layout/include_circle_picture_medium" />

    <TextView
        android:id="@+id/nome_profissional_movie"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_centerHorizontal="true"
        android:maxLines="1"
        android:fontFamily="sans-serif-light"
        android:textStyle="bold"
        android:layout_below="@id/person_image"
        android:textColor="@color/primary_text"/>

    <TextView
        android:id="@+id/subtitulo_item_person_movie"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:fontFamily="sans-serif-light"
        android:textSize="12dp"
        android:maxLines="1"
        android:layout_below="@id/nome_profissional_movie"
        android:layout_centerHorizontal="true"
        android:textColor="@color/secondary_text"/>

</RelativeLayout>